<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Logowanie | Rejestracja</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #4CAF50, #2E7D32);
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      width: 300px;
      text-align: center;
    }
    h2 {
      margin-bottom: 20px;
      color: #333;
    }
    input {
      width: 90%;
      padding: 10px;
      margin: 8px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
    }
    button {
      width: 100%;
      padding: 12px;
      margin-top: 10px;
      border: none;
      border-radius: 8px;
      background: #4CAF50;
      color: white;
      font-size: 1em;
      cursor: pointer;
    }
    button:hover {
      background: #45a049;
    }
    .toggle {
      margin-top: 15px;
      font-size: 0.9em;
      cursor: pointer;
      color: #2E7D32;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 id="form-title">Logowanie</h2>
    <input type="text" id="username" placeholder="Nazwa użytkownika">
    <input type="password" id="password" placeholder="Hasło">
    <div id="extra-field" style="display: none;">
      <input type="email" id="email" placeholder="Email">
    </div>
    <button onclick="submitForm()">Zaloguj</button>
    <div class="toggle" onclick="toggleForm()">Nie masz konta? Zarejestruj się</div>
  </div>

  <script>
    let isLogin = true;

    function toggleForm() {
      isLogin = !isLogin;
      document.getElementById("form-title").innerText = isLogin ? "Logowanie" : "Rejestracja";
      document.querySelector("button").innerText = isLogin ? "Zaloguj" : "Zarejestruj";
      document.getElementById("extra-field").style.display = isLogin ? "none" : "block";
      document.querySelector(".toggle").innerText = isLogin 
        ? "Nie masz konta? Zarejestruj się" 
        : "Masz już konto? Zaloguj się";
    }

    function getUsers() {
      return JSON.parse(localStorage.getItem("users") || "[]");
    }

    function saveUsers(users) {
      localStorage.setItem("users", JSON.stringify(users));
    }

    function submitForm() {
      const username = document.getElementById("username").value.trim();
      const password = document.getElementById("password").value.trim();
      const email = document.getElementById("email")?.value.trim();

      if (!username || !password || (!isLogin && !email)) {
        alert("Wypełnij wszystkie pola!");
        return;
      }

      let users = getUsers();

      if (isLogin) {
        // LOGOWANIE
        let user = users.find(u => u.username === username && u.password === password);
        if (user) {
          localStorage.setItem("loggedUser", username); // zapamiętaj kto jest zalogowany
          window.location.href = "home.html"; // przejście do strony głównej
        } else {
          alert("Błędny login lub hasło!");
        }
      } else {
        // REJESTRACJA
        let exists = users.some(u => u.username === username);
        if (exists) {
          alert("Taki użytkownik już istnieje!");
        } else {
          users.push({ username, password, email });
          saveUsers(users);
          alert("Rejestracja zakończona sukcesem! 🎉\nMożesz się zalogować.");
          toggleForm();
        }
      }
    }
  </script>
</body>
</html>
